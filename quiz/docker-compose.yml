version: '3'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: $DB_PASSWORD
      POSTGRES_USER: $DB_USER
      POSTGRES_DB: $DB_NAME
    volumes:
      - pgdb:/var/lib/postgresql/data
  redis:
    image: redis:latest
    restart: always
    hostname: redis
  web:
    build: .
    environment:
      DB_PASSWORD: $DB_PASSWORD
      DB_USER: $DB_USER
      DB_NAME: $DB_NAME
      SECRET_KEY: $SECRET_KEY
      PYTHONIOENCODING: utf-8
    command: python3 manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/quiz
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
  celery:
    build: .
    environment:
      DB_PASSWORD: $DB_PASSWORD
      DB_USER: $DB_USER
      DB_NAME: $DB_NAME
      SECRET_KEY: $SECRET_KEY
      PYTHONIOENCODING: utf-8
    command: celery -A questions worker --loglevel info
    volumes:
      - .:/quiz
    depends_on:
      - db
      - redis
    links:
      - redis
volumes:
  pgdb:
